# OmO-freebuff — Buffy's working notes (2026-08-15)

Dated note per the repo convention (avoid editing `OmO-claude/STATUS.md` —
Claude Code is also active on this repo).

## Continuation: finished the wiki wiring

Picked up where the last session stopped — the Memory Hub stack was up, the
`blueprint.sh` was written but uncommitted, and two wiki knowledge bases were
in flight.

### Done this session

1. **Committed `blueprint.sh`** to `OmO/core` (was untracked).
   `c26c724 feat: add executable finalize blueprint`.

2. **`agency-agents` wiki (`wiki-tw5ysnnv`) finished ingesting → allocated.**
   The 6-curated-persona wiki flipped to `ready` (37 pages generated, ~20 min
   on the free-tier OpenRouter LLM). Allocated to the 4 tdai-memory agents via
   `POST /api/v1/knowledge/allocate` (the panel endpoint — direct
   `agent-fixed-asset/set` calls return `501 NOT_IN_SCOPE`; the panel route is
   the correct path):
   - HQ Orchestrator `agt-22xb7h1qt7`
   - WhatsApp Agent Ops `agt-22uw3uv7zr`
   - Outreach & Sales `agt-22vr3ycxtf`
   - Client Success & Onboarding `agt-22wibafbgv`

3. **`omanai-playbooks` wiki (`wiki-5hp5bhvp`) — found 7 raw docs uploaded
   but ingestion never triggered** (status still `draft`). Triggered ingest:
   `POST /v3/wiki/ingest` → now `processing/ingesting`. Contains the real Drive
   operating docs (agency-master-playbook, client-onboarding-sop,
   pricing-and-proposal-template, pilot-to-paid-conversion-script,
   scopekeeper-outreach-pipeline, omanai-income-plan, whatsapp-agent-service-brief).

4. **Watcher script running in background** (`OmO-freebuff/scripts/
   wait-and-allocate-wiki.sh`, pid in `/tmp/wiki-allocate-watch.log`):
   polls the wiki until `ready`, then allocates to **every active agent in the
   team** (resolved dynamically — now 5, incl. the new AI Tools Outreach /
   Scopekeeper agent `agt-24if4hb2km` added by the Claude session). Log:
   `tail -f /tmp/wiki-allocate-watch.log`. ~4-5 min/doc × 7 docs ≈ 25-30 min.

## Decision: 3-offer conflict resolved → WhatsApp AI Agents leads

Full rationale in `OmO-freebuff/DECISION-lead-offer-2026-08-15.md` (also
committed). Summary:

- **Lead: WhatsApp AI Agents** (construction/aluminum/fit-out, Oman) — the only
  offer with a complete operating system (playbook, pricing OMR149–499,
  onboarding SOP, QA bar, escalation) **and** running code (Apex server up;
  Twilio connector coded, creds not yet set). Sellable after 1–3 days of
  configuration (Twilio creds + Meta Business API approval).
- **Side channel: Scopekeeper** — free-POC lead-gen for a second ICP; connect
  Apollo mailbox, keep moving with near-zero effort, never at the expense of
  WhatsApp. No email out without human review.
- **Parked: n8n automation service** — the income plan's "demo-ready" claim
  doesn't match disk (1 workflow, empty workflows dir, not running). Un-park
  only after WhatsApp has 1–2 paying clients AND the 5-workflow package exists.

Operationalized in Memory Hub: updated prompts for **HQ Orchestrator**
(prioritizes WhatsApp, parks n8n), **WhatsApp Agent Ops** (owns the lead line,
QA bar + escalation baked in), and **Outreach & Sales** (WhatsApp-first,
Scopekeeper secondary). Decision doc added to the `omanai-playbooks` wiki
raw store once ingestion finishes (write is blocked while processing).

## Verified environment state

- Memory Hub stack: all 4 containers up (memory-core :8420, hub :8125/:8424,
  proxy :8096), filtered dashboard image (`localhost/memory-hub-filtered`)
  running as the hub.
- MCP bridge (blueprint stage 1): graphify, chrome-devtools, GitKraken all
  connected (`claude mcp list`).
- Both wikis owned by admin `usr-2y5raz9qqm` under team `team-2z1n59cudp`.

## Open items (human decisions, not agent work)

- **Twilio creds + Meta WhatsApp Business API approval** — required to actually
  sell the lead offer. Set in `~/ai_company/.env`.
- **Apollo sending mailbox** — connect one so Scopekeeper can run.
- **`aj@` vs `as@` account ownership** — Apollo credits are tied to `aj@`.
- `omanai-playbooks` allocation lands automatically via the watcher; verify at
  `http://localhost:8125/#/wiki` once ingestion flips to Ready.
